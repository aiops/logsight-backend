import { OnChanges, SimpleChanges } from '@angular/core';
export declare class HighlightJsDirective implements OnChanges {
    private hljs;
    source: string;
    language: string;
    highlightedCode: string;
    constructor(hljs: any);
    ngOnChanges(changes: SimpleChanges): void;
}
